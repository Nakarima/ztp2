{% include 'default/_form.html.twig' with {
    page_title: 'title_bug_edit',
    page_title_param: {'%id%': bug.id},
    attr: {
        method: 'PUT',
        action: url('bug_edit', {'bugId': bug.id}),
    },
    form: form,
    back_to_list_path: 'bug_index',
} %}